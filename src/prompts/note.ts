// @/core/prompts/note/optimize.ts



export const NOTE_OPTIMIZE_PROMPT = `
你是一个专业的笔记优化助手，请根据提供的原始笔记内容，优化这个笔记的质量和结构。

【原始笔记内容】
{{originalContent}}

【附加语法说明】
{{}} 包裹的内容代表闪卡模式下的挖空内容，用来记忆关键概念


【优化任务】
请分析笔记内容，优化笔记的质量和结构。第一行将作为标题，其余内容作为笔记正文。

【优化原则】
1. 尊重原文原则：
   - 忠实于原始笔记的核心内容和意图
   - 不要添加原始内容中不存在的信息或扩展无关内容
   - 只在原有信息基础上进行组织和表达优化
   - 保持原笔记的知识范围和深度
   - 必须完整保留原文中的所有标签（如 #标签 格式），不要移除、修改或移动这些标签

2. 标题处理：
   - 如果原始内容有标题（第一行），请完整保留原标题，不要修改或优化
   - 如果原始内容没有标题，请根据内容生成一个合适的标题
   - 标题必须包含在输出中（第一行）
3. 内容优化：
   - 保持逻辑清晰，结构合理
   - 优化语言表达，使其更准确专业
   - 确保内容连贯性和可读性
   - 必须保留原文中的所有标签：如果原文中包含标签（如 #标签 格式），必须完整保留，不要移除、修改或移动这些标签
   - 格式保持原则（重要）：
     * 如果原始笔记是 Markdown 格式，优化后保持 Markdown 格式，但不要添加新的 Markdown 语法元素（如不要添加新的标题、列表、加粗等）
     * 如果原始笔记不是 Markdown 格式（如纯文本），优化后也保持纯文本格式，不要转换为 Markdown，不要添加任何 Markdown 语法
     * 严格遵循原文的格式类型，不要改变格式风格
     * 不要添加任何 Markdown 语法元素

【输出格式】
请直接返回优化后的内容，第一行是标题（不使用 # 符号），标题和内容之间用一个空行分隔，不要包含任何额外的说明、JSON格式或其他包装文本。

【注意事项】
- 直接返回优化后的内容文本，第一行是标题（不使用 # 符号），标题和内容之间用一个空行分隔
- 格式必须与原文保持一致（原文是Markdown则输出Markdown，原文是纯文本则输出纯文本）
- 不要使用JSON格式，不要添加任何包装文本
- 不要添加任何额外的说明文字
- 严格遵循尊重原文原则，不扩展无关内容
- 严格遵循格式保持原则，不要改变原文的格式类型
- 标签处理原则（重要）：
  * 必须完整保留原文中的所有标签（如 #标签 格式），不要移除、修改或移动这些标签
  * 不要添加任何新的标签，只保留原文中已存在的标签
`;



export const noteOptimizePrompt = (originalContent: string): string => {
    return NOTE_OPTIMIZE_PROMPT
        .replace('{{originalContent}}', originalContent);
};



export const parseNoteOptimization = (content: string): string | null => {
    try {
        const trimmed = content.trim();
        if (!trimmed) {
            return null;
        }
        return trimmed;
    } catch (error) {
        console.error('解析AI优化结果失败:', error);
        return null;
    }
};



// noteAdd 页面优化提示词（包含标题优化）
export const NOTE_ADD_OPTIMIZE_PROMPT = `
你是一个专业的笔记优化助手，请根据提供的原始笔记内容，优化这个笔记的质量和结构。

【原始笔记内容】
{{originalContent}}

【附加语法说明】
{{}} 包裹的内容代表闪卡模式下的挖空内容，用来记忆关键概念

【优化任务】
请分析笔记内容，优化笔记的质量和结构，包括标题和内容。

【优化原则】
1. 尊重原文原则：
   - 忠实于原始笔记的核心内容和意图
   - 不要添加原始内容中不存在的信息或扩展无关内容
   - 只在原有信息基础上进行组织和表达优化
   - 保持原笔记的知识范围和深度
   - 必须完整保留原文中的所有标签（如 #标签 格式），不要移除、修改或移动这些标签

2. 标题优化（重要）：
   - 如果原始内容没有标题，请根据内容生成一个简洁明了的标题
   - 如果原始内容已有标题，请优化标题使其更准确概括内容
   - 标题应该放在第一行，不使用 Markdown 的 # 符号
   - 标题和内容之间用空行分隔

3. 内容优化：
   - 保持逻辑清晰，结构合理
   - 优化语言表达，使其更准确专业
   - 确保内容连贯性和可读性
   - 必须保留原文中的所有标签：如果原文中包含标签（如 #标签 格式），必须完整保留，不要移除、修改或移动这些标签
   - 格式保持原则（重要）：
     * 如果原始笔记是 Markdown 格式，优化后保持 Markdown 格式，但不要添加新的 Markdown 语法元素（如不要添加新的标题、列表、加粗等）
     * 如果原始笔记不是 Markdown 格式（如纯文本），优化后也保持纯文本格式，不要转换为 Markdown，不要添加任何 Markdown 语法
     * 严格遵循原文的格式类型，不要改变格式风格
     * 不要添加任何 Markdown 语法元素（标题除外，标题放在第一行不使用 # 符号）

【输出格式】
请直接返回优化后的内容，格式为：第一行是标题（不使用 # 符号），空行，然后是内容，最后是标签（如果原文有标签）。

【注意事项】
- 第一行必须是标题（不使用 # 符号），标题和内容之间用空行分隔
- 直接返回优化后的内容文本，格式必须与原文保持一致（原文是Markdown则输出Markdown，原文是纯文本则输出纯文本）
- 不要使用JSON格式，不要添加任何包装文本
- 不要添加任何额外的说明文字
- 严格遵循尊重原文原则，不扩展无关内容
- 严格遵循格式保持原则，不要改变原文的格式类型
- 标签处理原则（重要）：
  * 必须完整保留原文中的所有标签（如 #标签 格式），不要移除、修改或移动这些标签
  * 不要添加任何新的标签，只保留原文中已存在的标签
`;

export const noteAddOptimizePrompt = (originalContent: string): string => {
    return NOTE_ADD_OPTIMIZE_PROMPT
        .replace('{{originalContent}}', originalContent);
};


// 多篇笔记的提示词
export const NOTES_OPTIMIZE_PROMPT = `
你是一个专业的笔记优化助手，请根据提供的原始笔记内容，优化这些笔记的质量和结构。
【原始笔记内容】
{{originalContent}}
【附加语法说明】
{{}} 包裹的内容代表闪卡模式下的挖空内容，用来记忆关键概念
==== 包裹的内容代表高亮的内容
【优化任务】
请分析笔记内容，然后直接返回优化后的内容。
【优化原则】
1. 尊重原文原则：
   - 忠实于原始笔记的核心内容和意图
   - 不要添加原始内容中不存在的信息或扩展无关内容
   - 只在原有信息基础上进行组织和表达优化
   - 保持原笔记的知识范围和深度
   - 必须完整保留原文中的所有标签（如 #标签 格式），不要移除、修改或移动这些标签
2. 内容优化：
   - 保持逻辑清晰，结构合理
   - 优化语言表达，使其更准确专业
   - 确保标题简洁明了，准确概括内容
   - 如果原始内容包含标题，请在优化后的内容中保留或优化它
   - 格式保持原则（重要）：
     * 如果原始笔记是 Markdown 格式，优化后保持 Markdown 格式，但不要添加新的 Markdown 语法元素（如不要添加新的标题、列表、加粗等）
     * 如果原始笔记不是 Markdown 格式（如纯文本），优化后也保持纯文本格式，不要转换为 Markdown，不要添加任何 Markdown 语法
     * 严格遵循原文的格式类型，不要改变格式风格
     * 不要添加任何 Markdown 语法元素
   - 标签处理原则（重要）：
     * 必须完整保留原文中的所有标签（如 #标签 格式），不要移除、修改或移动这些标签
     * 不要添加任何新的标签，只保留原文中已存在的标签

3. 保持专业性和准确性
6. 不要丢失原始内容的重要信息
7. 如果原始内容已经很完善，可以只做微调

【输出要求】
请直接返回优化后的内容，不要包含任何额外的说明、JSON格式或其他包装文本。
如果原始内容包含多篇笔记（用空行分隔），请保持多篇笔记的结构，为每篇笔记分别优化。

【格式要求（重要）】
- 严格遵循格式保持原则：如果原文是 Markdown 格式，输出也必须是 Markdown 格式；如果原文是纯文本格式，输出也必须是纯文本格式
- 不要改变原文的格式类型，不要将纯文本转换为 Markdown，也不要将 Markdown 转换为纯文本
- 不要额外添加任何 Markdown 语法元素（如标题、列表、加粗、斜体等）
- 标签处理原则（重要）：
  * 必须完整保留原文中的所有标签（如 #标签 格式），不要移除、修改或移动这些标签
  * 不要添加任何新的标签，只保留原文中已存在的标签
`;

export const notesOptimizePrompt = (originalContent: string): string => {
    return NOTES_OPTIMIZE_PROMPT
        .replace('{{originalContent}}', originalContent);
};


// 智能挖空提示词
export const NOTE_ADD_MASK_PROMPT = `
你是一个专业的笔记助手，请为笔记内容添加智能挖空，帮助记忆关键概念。

【原始笔记内容】
{{originalContent}}

【附加语法说明】
{{}} 包裹的内容代表闪卡模式下的挖空内容，用来记忆关键概念

【任务】
请分析笔记内容，为关键概念和重要知识点添加挖空语法 {{}}，帮助用户更好地记忆和理解。

【挖空原则】
1. 挖空选择：
   - 只对最核心、最关键的概念、术语、定义、公式、日期、人名、地名等关键信息进行挖空
   - 选择那些真正需要记忆和理解的重要知识点
   - 避免对显而易见或过于简单的内容挖空
   - 避免对连接词、助词等无意义内容挖空

2. 挖空数量：
   - 根据笔记长度和内容复杂度，合理添加挖空
   - 短笔记（少于200字）：1-2处挖空
   - 中等笔记（200-500字）：2-4处挖空
   - 长笔记（500字以上）：3-6处挖空
   - 不要过度挖空，保持内容的可读性

3. 挖空位置：
   - 优先选择定义、概念、关键术语
   - 选择需要记忆的数值、日期、人名
   - 选择重要的结论、要点
   - 避免在标题、标签等位置挖空

4. 格式保持：
   - 严格保持原文的格式（Markdown或纯文本）
   - 不要改变原文的结构和内容
   - 只添加挖空语法，不修改其他内容
   - 保持原文的标题、段落、列表等格式

【输出格式】
请直接返回添加挖空后的内容，不要包含任何额外的说明、JSON格式或其他包装文本。

【注意事项】
- 直接返回添加挖空后的内容文本，格式必须与原文保持一致（原文是Markdown则输出Markdown，原文是纯文本则输出纯文本）
- 不要使用JSON格式，不要添加任何包装文本
- 不要添加任何额外的说明文字
- 只添加 {{}} 挖空语法，不修改其他内容
- 不要修改原文的结构和表达
- 不要添加标签或其他额外内容
- 严格遵循格式保持原则
`;


export const noteAddMaskPrompt = (originalContent: string): string => {
    return NOTE_ADD_MASK_PROMPT
        .replace('{{originalContent}}', originalContent);
};


// 续写提示词
export const NOTE_CONTINUE_PROMPT = `
你是一个专业的笔记助手，请根据提供的笔记内容，自然地续写内容。

【当前笔记内容】
{{originalContent}}

【附加语法说明】
{{}} 包裹的内容代表闪卡模式下的挖空内容，用来记忆关键概念

【续写任务】
请分析当前笔记内容，自然地续写内容，使笔记更加完整和深入。

【续写原则】
1. 内容连贯性：
   - 续写内容应该与原文自然衔接，保持逻辑连贯
   - 延续原文的主题和思路，不要偏离主题
   - 保持内容的流畅性和可读性

2. 风格一致性：
   - 保持与原文相同的语言风格和表达方式
   - 如果原文是学术风格，续写也保持学术风格
   - 如果原文是通俗易懂的风格，续写也保持通俗易懂

3. 格式保持：
   - 严格保持原文的格式（Markdown或纯文本）
   - 不要改变原文的格式类型
   - 不要添加新的 Markdown 语法元素（如标题、列表、加粗等）
   - 续写内容直接接在原文后面，不需要空行分隔

4. 内容深度：
   - 可以扩展原文的观点，提供更多细节或例子
   - 可以补充相关的知识点或信息
   - 但不要添加与主题无关的内容
   - 保持内容的准确性和专业性

5. 标签处理：
   - 不要添加或修改标签
   - 如果原文有标签，续写时不要提及标签

【输出格式】
请直接返回续写的内容，不要包含任何额外的说明、JSON格式或其他包装文本。

【注意事项】
- 只返回续写的内容，不要重复原文
- 续写内容直接接在原文后面，保持格式一致
- 不要使用JSON格式，不要添加任何包装文本
- 不要添加任何额外的说明文字
- 严格遵循格式保持原则，不要改变原文的格式类型
- 不要添加标签或其他额外内容
`;

export const noteContinuePrompt = (originalContent: string): string => {
    return NOTE_CONTINUE_PROMPT
        .replace('{{originalContent}}', originalContent);
};


// 生成标题提示词
export const NOTE_GENERATE_TITLE_PROMPT = `
你是一个专业的笔记助手，请根据提供的笔记内容，生成一个简洁明了的标题。

【笔记内容】
{{originalContent}}

【任务】
请分析笔记内容，生成一个简短、准确、能概括核心内容的标题。

【标题要求】
1. 简洁明了：标题长度控制在10-20字以内，避免过长
2. 准确概括：标题应该准确反映笔记的核心内容和主题
3. 避免冗余：不要使用"关于"、"介绍"等冗余词汇
4. 专业准确：使用准确的专业术语，避免模糊表达

【输出格式】
请直接返回标题，不要包含任何额外的说明、引号、JSON格式或其他包装文本。只返回标题文本本身。

【注意事项】
- 只返回标题文本，不要添加任何前缀、后缀或说明
- 不要使用引号包裹标题
- 不要使用JSON格式
- 不要添加任何额外的说明文字
- 标题应该是一个简洁的短语，不是完整的句子
`;

export const noteGenerateTitlePrompt = (originalContent: string): string => {
    return NOTE_GENERATE_TITLE_PROMPT
        .replace('{{originalContent}}', originalContent);
};


// 只优化内容的提示词（不修改标题）
export const NOTE_OPTIMIZE_CONTENT_ONLY_PROMPT = `
你是一个专业的笔记优化助手，请根据提供的笔记内容，优化笔记的质量和结构。注意：只优化内容部分，不要修改标题。

【原始笔记内容】
{{originalContent}}

【附加语法说明】
{{}} 包裹的内容代表闪卡模式下的挖空内容，用来记忆关键概念

【优化任务】
请分析笔记内容，只优化内容部分（不包括标题），保持标题不变。

【优化原则】
1. 尊重原文原则：
   - 忠实于原始笔记的核心内容和意图
   - 不要添加原始内容中不存在的信息或扩展无关内容
   - 只在原有信息基础上进行组织和表达优化
   - 保持原笔记的知识范围和深度
   - 必须完整保留原文中的所有标签（如 #标签 格式），不要移除、修改或移动这些标签

2. 标题处理（重要）：
   - 如果原始内容有标题（第一行），请完整保留原标题，不要修改或优化
   - 标题必须包含在输出中（第一行），且与原文完全一致

3. 内容优化：
   - 保持逻辑清晰，结构合理
   - 优化语言表达，使其更准确专业
   - 确保内容连贯性和可读性
   - 必须保留原文中的所有标签：如果原文中包含标签（如 #标签 格式），必须完整保留，不要移除、修改或移动这些标签
   - 格式保持原则（重要）：
     * 如果原始笔记是 Markdown 格式，优化后保持 Markdown 格式，但不要添加新的 Markdown 语法元素（如不要添加新的标题、列表、加粗等）
     * 如果原始笔记不是 Markdown 格式（如纯文本），优化后也保持纯文本格式，不要转换为 Markdown，不要添加任何 Markdown 语法
     * 严格遵循原文的格式类型，不要改变格式风格
     * 不要添加任何 Markdown 语法元素

【输出格式】
请直接返回优化后的内容，第一行是标题（与原文完全一致，不使用 # 符号），标题和内容之间用一个空行分隔，不要包含任何额外的说明、JSON格式或其他包装文本。

【注意事项】
- 直接返回优化后的内容文本，第一行是标题（与原文完全一致，不使用 # 符号），标题和内容之间用一个空行分隔
- 格式必须与原文保持一致（原文是Markdown则输出Markdown，原文是纯文本则输出纯文本）
- 不要使用JSON格式，不要添加任何包装文本
- 不要添加任何额外的说明文字
- 严格遵循尊重原文原则，不扩展无关内容
- 严格遵循格式保持原则，不要改变原文的格式类型
- 标题必须与原文完全一致，不要修改
- 标签处理原则（重要）：
  * 必须完整保留原文中的所有标签（如 #标签 格式），不要移除、修改或移动这些标签
  * 不要添加任何新的标签，只保留原文中已存在的标签
`;

export const noteOptimizeContentOnlyPrompt = (originalContent: string): string => {
    return NOTE_OPTIMIZE_CONTENT_ONLY_PROMPT
        .replace('{{originalContent}}', originalContent);
};